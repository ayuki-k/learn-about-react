# Dockerfile

# Base image
FROM node:20-slim

# Set working directory inside the container
WORKDIR /app

# Install dependencies
COPY package*.json ./
RUN npm install

# Start the Next.js application
CMD ["npm", "run", "dev"]
